.button-row {
	flex-direction: row;
	justify-content: space-between;
	align-items: center;
	background-color: var(--unity-base-background-color);
}

.button-row > .unity-button {
	flex: 1;
	margin: 2px 2px;
}